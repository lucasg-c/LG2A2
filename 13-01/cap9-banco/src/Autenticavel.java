public interface Autenticavel {
    //interface ''conversa'' com um objeto
    //define métodos mas nunca contém a implementação deles,
    //  isto é, define o que um objeto deve fazer, mas não ''como''
    //são as outras classes que implementam a interface
    
    boolean autentica (int senha);
}
